<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.button :submit %>
<% end %>

<%= link_to 'Home', restaurants_path, class: "btn btn-primary" %>
